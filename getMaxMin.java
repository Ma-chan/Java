void getMaxMin(String[] args){
           int max=arr[0];
           int min=arr[1];
           for(int i=0;i<arr.length;i++){
               int v=arr[i];
           if(v>max){
               max=v;
           }
           if(v<min){
               min=v;
           }
           }
